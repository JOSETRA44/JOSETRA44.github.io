/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  trailingSlash: true,
  basePath: "/JOSETRA44",
  assetPrefix: "/JOSETRA44/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
